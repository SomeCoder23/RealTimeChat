version: '3'
services:
  app:
    profiles:
      - in_docker
    container_name: realtimechat-1-web-1
    build: .
    environment:
      - PORT=80
      - DB_HOST=localhost
      - DB_USER_NAME=root
      - DB_PASSWORD=''
      - DB_NAME=chatapp
    ports:
      - "5000:80"